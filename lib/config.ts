/**
 * Percentage cut taken from each order, as a decimal (0.01 = 1%).
 * Change this single number whenever your fee changes — everywhere
 * that shows "you'll receive" or "service fee" reads from here.
 */
export const SERVICE_FEE_PERCENT = 0.01;
